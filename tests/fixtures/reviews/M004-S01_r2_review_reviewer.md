# Review: M004-S01 round 2

## Findings
| id | severity | disposition | summary |
| --- | --- | --- | --- |
| M004-S01-01 | P2 | closed_by_review | Closed. `qualification.md:250-259` now states that the coder cost contributions are absent from the copied `coded` rows (confirmed: `ledger.jsonl:2` and `:17` carry no `cost_usd` key), names their source as each coder job's local `result.json` per `commands_and_versions.txt:62-64`, and reconciles $0.104768 + $0.03901 = $0.143778 (printed `0.14377800000000002`) and $0.1488335 + $0.010123 = $0.1589565, totaling $0.3027345, against `status_usage.txt:5-8`. Every figure is cited to `commands_and_versions.txt:62-69`, which is inside the qualification evidence set; the arithmetic checks out and no number is now unsourced. |
| M004-S01-11 | P2 | closed_by_review | Closed by the same text. The document no longer leaves the 0.03901 and 0.010123 deltas unexplained: `qualification.md:250-252` says explicitly that the copied `coded` ledger rows carry no cost and that `status --usage` reads it from the job's `result.json`, so the "not observed" clause is satisfied by a cited source rather than by silence. |
| M004-S01-21 | P2 | closed_by_review | Closed, and within the constraint the finding set. `qualification.md:255-256` marks the M001-S02 coder cost as the last-message cost only (D022), and `qualification.md:265-268` still scopes the corrected cost total and the cache-write total as not observed *in this qualification evidence set*, so the document does not import the `tests/fixtures/samples/pi_canary_usage.jsonl` totals it may not cite. |
| M004-S01-03 | P3 | closed_by_review | Addressed this round: the `pi_canary_usage.jsonl` row in `samples/README.md` now names Pi 0.85.0 and links it to `commands_and_versions.txt:6`, making the sample table self-describing against the 0.84.4 originals. |
| M004-S01-22 | P3 | closed_by_review | Addressed this round: `qualification.md:221, 226` now give the prompt-to-stop intervals the ledger supports (20 s from `ledger.jsonl:14-15`, 56 s from `ledger.jsonl:23-24`) and label them "at whole-second ledger precision" rather than as upper bounds, which is correct because the recorded durations 20.126 s and 56.208 s exceed the truncated whole-second differences. Run 2b remains "not observed", correctly, since no round-1 prompt row precedes its stop. |
| M004-S01-23 | P3 | carried | Unchanged this round. The D018 section still quotes only the six `node.exe` rows of `tasklist_after_kills.txt` and does not record the two `python.exe` and two `claude.exe` rows on lines 5, 7, 13, 18 of the same listing. The acceptance asks only about node and pi, so this stays backlog-quality. |
| M004-S01-04 | P3 | open | The new evidence section records more about M001-S03 than the document uses. `commands_and_versions.txt:66-67` gives the M001-S03 coder job as 0.012919 USD at 56.208 s with "no reviewed row, so it appears in no status row"; `qualification.md:226` takes the 56.208 s from that line, but the usage-limits paragraph at `qualification.md:275-276` reports only that M001-S03's status quantities are all `?`. One clause noting that a coder cost was recorded for M001-S03 and excluded from the status row would complete the section's account of the limits. Nothing stated is wrong, and "a total covering every attempted job is not observed" remains true. |

## Closure Decision
Objective status: achieved
Objective evidence: Every acceptance clause is met against the copied evidence — the six M001-S01 ledger rows, both garbled Git status lines, the status sums, the command record, and the node rows are byte-verbatim quotations of `ledger.jsonl:1-4, 11-12`, `run_02_timeout.txt:4`, `run_03_violation.txt:4`, `status_usage.txt:5-8`, `commands_and_versions.txt:22-40`, and `tasklist_after_kills.txt` (the UTF-16LE-to-UTF-8 recovery reproduces its source line exactly in both cases), the README now states the per-message D022 summation and carries the redaction-described `pi_canary_usage.jsonl` row, and the receipt records the full hermetic run at exit 0 with 287 passed over changed files whose sha256 values match the manifest and the working tree.

## Verdict
Verdict: pass - next: architect records this review and accepts M004-S01, carrying M004-S01-04 and M004-S01-23 to backlog.
